# Skyrovix Task 3 - Expense Tracker

**Student:** Nirali Singh

SQLite CLI expense tracker with CRUD, categories, monthly reports and budget alerts.

## Run
`python -m expense_tracker`

The local `expenses.db` is generated automatically and ignored by Git.

## Test
`python -m unittest discover -s tests`
